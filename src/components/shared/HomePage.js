import React from 'react';
import GroupsList from '../groups/GroupsList';

const HomePage = () => (
    <div>
        <h1> Home Page </h1>
        <GroupsList/>
    </div>
);

export default HomePage;
